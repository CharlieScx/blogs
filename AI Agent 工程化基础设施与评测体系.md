这是在构建 AI Agent 工程化基础设施与评测体系，旨在将 Agent 开发从“Prompt 拼凑”阶段推进到可交付、可验证、可治理的生产级工程实践。具体包含以下核心工作：

实现 Task Contract 评审器： 构建自动化校验机制，确保任务契约（定义输入输出、边界约束、验收标准）的完整性与合规性，防止模糊需求直接进入执行环节，是质量左移的关键门禁 。
实现最小 Agent Loop 状态机： 搭建 Plan → Act → Observe → Evaluate → Finish 的核心执行循环，为 Agent 提供确定性的运行时骨架，解决纯 ReAct 模式下的步骤缺失与语义漂移问题 。
为工具调用定义统一结果协议： 规范 CLI、MCP、Skill 等异构工具的返回格式（如成功/失败结构、错误码、元数据），消除集成歧义，支撑跨工具编排与自动化评估 。
建立 20 个正常与失败 Eval 样本： 构建覆盖正向路径与典型故障（如幻觉、死循环、参数错误）的基准测试集，用于量化验证 Agent 行为是否符合契约预期，驱动迭代优化 。
比较 CLI、MCP、Skill 在同一任务中的职责： 通过对照实验厘清三类工具的能力边界与适用场景（如 CLI 适合本地原子操作、MCP 适合标准化服务接入、Skill 适合封装业务逻辑），避免能力重叠或误用，指导架构选型 。
整体目标是将 Agent 从“能跑通”升级为“可交付”，通过契约约束、状态可控、协议统一、评测闭环和职责清晰五大支柱，建立可复制的组织级 Agent 工程能力 。这标志着从框架调用迈向系统工程的关键转折 。