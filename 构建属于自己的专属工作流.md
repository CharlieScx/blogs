搭建“专属工作流”的关键，不是堆工具，而是把你每天重复做的事情固化成一条可复用流水线：

> 需求进入 → 理解项目 → 拆解任务 → 编码 → 验证 → 提交 → 复盘沉淀

## 一套推荐架构

### 1. 统一工作入口

建议每个项目至少保留：

```text
project/
├── README.md          # 项目入口、启动方式
├── AGENTS.md          # 给 AI/协作者的开发规则
├── docs/
│   ├── architecture.md
│   └── decisions/
├── scripts/           # 高频操作脚本
├── Makefile           # 统一命令入口
└── .env.example
```

将命令统一成：

```bash
make setup
make dev
make test
make lint
make build
make check
```

不要让自己每次都重新回忆启动参数。

### 2. 固定任务处理模板

收到需求后，先形成一个很短的任务卡：

```markdown
目标：
范围：
不做什么：
涉及模块：
验证方式：
风险：
```

任务较大时再拆成：

```text
调研 → 方案 → 接口 → 数据层 → UI → 测试 → 文档
```

判断规则可以非常简单：

- 单文件、小修改：直接开发
- 跨模块：先写任务清单
- 跨前后端：先定接口契约
- 架构或数据模型变化：先写方案
- 线上问题：先复现和诊断，再决定是否修复

### 3. 终端工作台

我偏爱这种朴素但稳定的组合：

- `zsh`：Shell
- `zoxide`：快速目录跳转
- `fzf`：模糊搜索
- `rg`：全文搜索
- `fd`：文件查找
- `tmux`：保持长期会话
- `direnv`：项目环境变量
- `jq`：处理 JSON
- `gh`：GitHub 工作流
- Docker/容器：隔离依赖

一个项目可以固定为三个终端窗口：

```text
editor | dev server | tests/logs
```

### 4. Git 工作流

保持可预测：

```text
main
 └── feat/xxx
 └── fix/xxx
```

推荐节奏：

1. 开始前检查 `git status`
2. 创建任务分支
3. 小步提交
4. 提交前运行 `make check`
5. PR 中写清：改了什么、如何验证、有什么风险

提交信息示例：

```text
feat(auth): add refresh token rotation
fix(order): prevent duplicate payment callback
refactor(api): centralize error handling
```

### 5. AI 协作分层

不要只让 AI “帮我写代码”，而是让它承担不同角色：

- 探索：定位文件、理解调用链
- 规划：拆任务、识别风险
- 实现：完成明确范围内的修改
- 审查：检查逻辑、边界和安全问题
- 验证：执行测试、构建、接口检查
- 沉淀：更新文档、脚本和项目规则

项目里的 `AGENTS.md` 可以写：

```markdown
## 技术栈
React + TypeScript + Node.js + PostgreSQL

## 开发规则
- 不直接修改生成文件
- 新接口必须补充测试
- 前端请求统一放在 src/services
- 禁止使用 any
- 修改数据库必须提供迁移脚本

## 验证命令
- npm run lint
- npm run test
- npm run build
```

### 6. 自动化优先级

最值得自动化的是：

- 初始化项目环境
- 启动多个服务
- 格式化、Lint、类型检查
- 测试与构建
- 数据库迁移
- 日志查询
- 分支创建与提交检查
- PR 描述生成
- 发布和回滚

判断是否值得自动化：

> 一周重复两次以上，或者每次都容易出错，就写成脚本。

### 7. 每周沉淀

每周花十分钟回答三个问题：

- 哪一步最浪费时间？
- 哪个错误重复出现？
- 哪条经验应该写进脚本、文档或 AI 规则？

最终你会得到三层资产：

```text
个人层：Shell 配置、命令别名、通用 AI 技能
团队层：开发规范、PR 模板、CI/CD
项目层：AGENTS.md、Makefile、架构决策和脚本
```

最小落地版本只需要先做四件事：建立 `AGENTS.md`、统一 `make` 命令、固定任务卡模板、配置提交前自动检查。工作流不是一次设计完成的，而是把每一次“不爽”逐渐改造成自动化。